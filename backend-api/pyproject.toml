[project]
name = "dashboard-backend-api"
version = "0.1.0"
description = "API service for the Liaoning franchise contribution dashboard"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "psycopg[binary]>=3.2",
  "pydantic>=2.8",
  "uvicorn>=0.30"
]

